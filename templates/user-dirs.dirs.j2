# This file is written by xdg-user-dirs-update
# If you want to change or add directories, just edit the line you're
# interested in. All local changes will be retained on the next run.
# Format is XDG_xxx_DIR="$HOME/yyy", where yyy is a shell-escaped
# homedir-relative path, or XDG_xxx_DIR="/yyy", where /yyy is an
# absolute path. No other format is supported.

# xdg-user-dirs is a tool to help manage "well known" user directories like the desktop folder and the music folder. It also handles localization (i.e. translation) of the filenames. 
# https://www.freedesktop.org/wiki/Software/xdg-user-dirs/

XDG_DESKTOP_DIR="{{ xdg_user_dirs_desktop }}"
XDG_DOWNLOAD_DIR="{{ xdg_user_dirs_download }}"
XDG_TEMPLATES_DIR="{{ xdg_user_dirs_templates }}"
XDG_DOCUMENTS_DIR="{{ xdg_user_dirs_documents }}"
XDG_PICTURES_DIR="{{ xdg_user_dirs_pictures }}"
XDG_PUBLICSHARE_DIR="{{ xdg_user_dirs_publicshare }}"
XDG_MUSIC_DIR="{{ xdg_user_dirs_music }}"
XDG_VIDEOS_DIR="{{ xdg_user_dirs_videos }}"